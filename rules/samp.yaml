rules:
  - id: insecure-strcpy-usage
    languages:
      - c
      - cpp
    message: Avoid using strcpy; it can cause buffer overflows. Consider safer alternatives like strncpy.
    severity: WARNING
    pattern: strcpy($DEST, $SRC)
    metadata:
      category: security
      confidence: HIGH
      technology:
        - c
        - cpp
